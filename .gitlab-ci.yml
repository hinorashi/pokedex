# image: busybox:latest
image: ubuntu:18.04

before_script:
  - echo "Before script section"
#   - echo "For example you might run an update here or install a build dependency"
#   - echo "Or perhaps you might print out some debugging details"
  - echo "install jdk 11 and maven..."
  - apt-get update -qq && apt-get install openjdk-11-jre-headless -qy && apt install maven -qy
  - java --version
  - mvn -v

after_script:
  - echo "After script section"
  - echo "For example you might do some cleanup here"

maven-build:
  stage: build
  script:
    - echo "We should compile and test to save time..."
    - mvn clean test
    - echo "Let's see what we've got..."
    - ls -alF

# test1:
#  stage: test
#  script:
#     - echo "Do a test here"
#     - echo "Unit test..."
#     - mvn test
#     - echo "For example run a test suite"

heroku:
  stage: deploy
  script:
    # - echo "Deploy..."
    # - mvn clean package
    - echo "Deploy to Heroku..."
    - gem install dpl
    - dpl --provider=heroku --app=hino-pokedex --api-key=$HEROKU_STAGING_API_KEY
    - echo "Let's see what we've got..."
    - ls -alF
